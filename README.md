# Consigna 21 para el curso de Coderhouse.

## Rutas

estas son de la api

- `GET` /api/products
- `POST` /api/products
- `GET` /api/products/:pid
- `PUT` /api/products/:pid
- `DELETE` /api/products/:pid

## Uso

```bash
$ npm i
$ npm start
$ npm test
```
